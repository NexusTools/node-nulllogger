import { INullLogger, LoggerLevel } from "../../types";
declare const _default: {
    new (): {
        filename: string;
        inspect0: any;
        buildScopeCache: any;
        extendEnv(env: any): void;
        log(level: LoggerLevel, logger: INullLogger, messages: any[]): void;
    };
};
export = _default;
